
[build]
builder = "NIXPACKS"
buildCommand = "rm -rf node_modules .npm && npm cache clean --force && npm install --include=dev && npm run build"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10